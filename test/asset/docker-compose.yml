version: '3.9'
services:
  db:
    image: postgres:alpine
    environment:
      - POSTGRES_PASSWORD=migratum-test
      - POSTGRES_DB=migratum-test
      - POSTGRES_USER=migratum-test
    ports:
      - 5432:5432
